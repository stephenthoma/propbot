query Proposals {
  proposals(
    first: 25,
    skip: 0,
    where: {state: "open"},
    orderBy: "created",
    orderDirection: desc
  ) {
    id
    title
    start
    created
    end
    snapshot
    state
    author
    space {
      id
      name
      twitter
    }
  }
}
