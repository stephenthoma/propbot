query Proposal($id: String) {
  proposal(id: $id) {
    id
    title
    start
    created
    end
    snapshot
    network
    choices
    strategies {
      name
      params
    }
    state
    author
    space {
      id
      name
      members
      twitter
    }
  }
}
